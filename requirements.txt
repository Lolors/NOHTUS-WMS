# NOHTUS WMS 실행 의존성
# 현재 운영 환경(run_wms.bat이 사용하는 python -m streamlit)에 설치된 버전으로 고정했다.
# 새 버전으로 올릴 때는 전체 테스트(python -m unittest discover -s tests -p "test_*.py")를
# 통과한 뒤에만 반영한다.

streamlit==1.58.0
streamlit-keyup==0.3.0
pandas==2.3.3
openpyxl==3.1.5
xlrd==2.0.2
Pillow==12.3.0
reportlab==4.5.1

# 선택 의존성: ERP 엑셀 파일의 워크시트 이름이 깨져 xlrd/openpyxl로 읽지 못할 때
# nohtus/services/stock_compare.py의 _read_with_calamine()이 폴백으로 사용한다.
# 설치되어 있지 않아도 앱은 정상 동작하며, ImportError 시 조용히 폴백을 건너뛴다.
# python_calamine

# nohtus.ui

Shared Streamlit UI helpers will move here gradually.

Suggested modules:

- layout.py: common column/layout helpers
- formatters.py: display labels and table formatting
- widgets.py: reusable inputs and selectors

Do not put DB writes in UI helper modules.
